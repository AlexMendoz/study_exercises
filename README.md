# Pequeño proyecto para estudiar y hacer ejercicios en typeScript

Tiene algo de Rust (wakala)
Tiene algo de TypeScript (esta mejor) 

Se tiene que ejecutar `npm i`.

Los comandos para ejecutar un script son `npx ts-node ruta/script.ts`.
